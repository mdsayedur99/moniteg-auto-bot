<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Razu safe earn pro</title>

<script src="https://telegram.org/js/telegram-web-app.js"></script>

<style>
@keyframes glow {
  0% { box-shadow: 0 0 10px #00e5ff; }
  50% { box-shadow: 0 0 30px #00e5ff; }
  100% { box-shadow: 0 0 10px #00e5ff; }
}
@keyframes pulse {
  0% { transform: scale(1); }
  50% { transform: scale(1.05); }
  100% { transform: scale(1); }
}
@keyframes spin {
  100% { transform: rotate(360deg); }
}
@keyframes dots {
  0% { content: ""; }
  33% { content: "."; }
  66% { content: ".."; }
  100% { content: "..."; }
}

body {
  margin: 0;
  height: 100vh;
  font-family: 'Segoe UI', sans-serif;
  background: radial-gradient(circle at top, #1a2a6c, #000);
  display: flex;
  justify-content: center;
  align-items: center;
  color: #fff;
}

.card {
  width: 350px;
  padding: 25px 20px;
  border-radius: 22px;
  background: rgba(0,0,0,0.55);
  text-align: center;
  animation: glow 3s infinite;
}

.mode-buttons {
  display: flex;
  gap: 10px;
  margin-bottom: 12px;
}

.mode-buttons button {
  flex: 1;
  padding: 10px;
  border-radius: 12px;
  border: none;
  font-size: 13px;
  font-weight: bold;
  cursor: pointer;
  color: #fff;
}

.auto-btn {
  background: linear-gradient(45deg, #00e5ff, #1de9b6);
}

.manual-btn {
  background: linear-gradient(45deg, #ff9800, #ff5722);
}

.profile {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  margin-bottom: 10px;
}

.profile img {
  width: 48px;
  height: 48px;
  border-radius: 50%;
  border: 2px solid #00e5ff;
}

.profile span {
  font-size: 15px;
  font-weight: 600;
}

.card h1 {
  font-size: 21px;
  color: #00e5ff;
  margin: 6px 0;
}

.card p {
  font-size: 13px;
  opacity: 0.9;
}

.loader {
  margin: 14px auto;
  width: 55px;
  height: 55px;
  border: 5px solid rgba(255,255,255,0.2);
  border-top: 5px solid #00e5ff;
  border-radius: 50%;
  animation: spin 1s linear infinite;
}

.loading-text {
  font-size: 14px;
  color: #ffeb3b;
}

.loading-text span::after {
  content: "";
  animation: dots 1.5s infinite;
}

.timer {
  margin-top: 12px;
  font-size: 18px;
  font-weight: bold;
  color: #4caf50;
  animation: pulse 1.5s infinite;
  display: none;
}

.watch-ads {
  display: none;
  margin-top: 15px;
}

.watch-ads button {
  width: 100%;
  padding: 14px;
  border-radius: 16px;
  border: none;
  background: linear-gradient(45deg, #4caf50, #00e676);
  font-size: 16px;
  font-weight: bold;
  color: #000;
  cursor: pointer;
  animation: pulse 1.5s infinite;
}
</style>
</head>

<body>

<div class="card">

  <div class="mode-buttons">
    <button class="auto-btn" onclick="enableAuto()">Auto Mathod ON</button>
    <button class="manual-btn" onclick="enableManual()">Manually ON</button>
  </div>

  <div class="profile">
    <img id="tgPhoto" src="https://i.imgur.com/6VBx3io.png">
    <span id="tgName">Telegram User</span>
  </div>

  <p>⭐ Points: <span id="pointBox">0</span></p>

  <h1>🚀 Razu safe earn pro</h1>
  <p>Auto & Manual Ads System</p>

  <div class="loader"></div>

  <div class="loading-text">
    Ad Playing<span></span>
  </div>

  <div class="timer" id="timerBox">
    Next Ads in <span id="countdown">18</span>s
  </div>

  <div class="watch-ads" id="watchAdsBox">
    <button onclick="showAdInstant()">🎬 Watch Ads</button>
  </div>

</div>

<!-- SDK -->
<script src="//libtl.com/sdk.js"
        data-zone="11210869"
        data-sdk="show_11210869"></script>

<script>
const tg = window.Telegram.WebApp;
tg.ready();

const tgName = document.getElementById("tgName");
const tgPhoto = document.getElementById("tgPhoto");
const countdown = document.getElementById("countdown");
const timerBox = document.getElementById("timerBox");
const watchAdsBox = document.getElementById("watchAdsBox");
const pointBox = document.getElementById("pointBox");

/* POINT SYSTEM */
let points = localStorage.getItem("points") ? parseInt(localStorage.getItem("points")) : 0;

function updatePointsUI() {
  pointBox.innerText = points;
}

function addPoint() {
  points += 1;
  localStorage.setItem("points", points);
  updatePointsUI();
}

/* TELEGRAM USER */
if (tg.initDataUnsafe?.user) {
  const u = tg.initDataUnsafe.user;
  tgName.innerText = u.first_name + (u.last_name ? " " + u.last_name : "");
  if (u.photo_url) tgPhoto.src = u.photo_url;
}

let timerInterval;
let timeLeft = 18;

/* =========================
   ✅ FIXED SDK SAFE AD CALL
   ========================= */
function showAdInstant() {
  try {
    if (typeof show_11210869 === "function") {
      const res = show_11210869();

      // safe promise check
      if (res && typeof res.then === "function") {
        res.then(() => addPoint()).catch(() => {});
      } else {
        addPoint();
      }
    } else {
      console.log("SDK not loaded yet");
    }
  } catch (e) {
    console.log("Ad error:", e);
  }
}

/* AUTO MODE */
function startAutoTimer() { clearInterval(timerInterval); timeLeft = 18; countdown.textContent = timeLeft; timerBox.style.display = "block"; timerInterval = setInterval(() => { timeLeft--; countdown.textContent = timeLeft; if (timeLeft <= 0) { clearInterval(timerInterval); location.reload(); } }, 1000); } function enableAuto() { localStorage.setItem("adMode", "auto"); watchAdsBox.style.display = "none"; showAdInstant(); startAutoTimer(); }

function enableAuto() {
  localStorage.setItem("adMode", "auto");
  watchAdsBox.style.display = "none";
  showAdInstant();
  startAutoTimer();
}

function enableManual() {
  localStorage.setItem("adMode", "manual");
  clearInterval(timerInterval);
  timerBox.style.display = "none";
  watchAdsBox.style.display = "block";
}

window.addEventListener("load", () => {
  updatePointsUI();

  if (localStorage.getItem("adMode") === "auto") {
    enableAuto();
  }
});
</script>

</body>
</html>